{
  "version": 2,
  "builds": [
    { "src": "backend/index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/backend/index.js" }
  ],
  "buildCommand": "echo 'Building minimal backend' && ls -la backend/",
  "outputDirectory": "backend"
}
